def getData(msg):
    data = input(msg)
    return data

def removeUpToN(str, n):
    str = str[n:]
    print(str)

str = getData("Type a string: ")
n = int(getData("Type n < len(str): "))
removeUpToN(str, n)
