def getData(msg):
    data = input(msg)
    return data

def removeUpToN(str):
    str = str[int(getData("Type n < len(str): ")):]
    return str

str = getData("Type a string: ")
print(removeUpToN(str))
