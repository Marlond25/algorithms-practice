def getStr():
    str = input()
    return str


def characterValidation(str):
    for i in range(len(str)):
        if i % 2 == 0:
            print(str[i], i)

str = getStr()
characterValidation(str)
